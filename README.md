# Proyecto Urban Grocers 
git config --global user.name "Javier Anizar"
git config --global user.email "pjanizar@gmail.com"
git add README.md
git commit -m "Commit inicial: archivo README"
git push -u origin main